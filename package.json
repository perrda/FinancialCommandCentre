{
  "name": "fcc",
  "version": "8.0.0-alpha.0",
  "private": true,
  "type": "module",
  "description": "Financial Command Centre — production-grade personal finance PWA",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm -r build",
    "preview": "pnpm --filter web preview",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean && rm -rf node_modules"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=22.13.0",
    "pnpm": ">=11.0.0"
  },
  "packageManager": "pnpm@11.0.9"
}
