{
  "name": "@fcc/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "echo 'shared is source-only — no build needed'",
    "test": "echo 'no tests yet — added in Phase 1'",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'lint placeholder'",
    "clean": "echo 'nothing to clean'"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
