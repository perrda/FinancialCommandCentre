/**
 * @fcc/shared — types, schemas, and pure utilities shared between
 * the web app and Cloudflare Workers.
 *
 * Phase 0: empty skeleton.
 * Phase 1: domain types (Profile, Holding, Liability, Snapshot, …)
 *          + Zod schemas + format/parse utilities.
 */

export const SHARED_VERSION = '0.0.0';
